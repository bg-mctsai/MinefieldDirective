# Commit Message

## Format

**單一變更**：`[Title] 敘述`（同一行）

**多項變更**：第一行僅 `[Title]`，**禁止**在 Title 後加敘述；body 以 `1. 2. 3.` 列點換行

## Title Rules

- Title = 變更的機制或元件名稱（精準對應程式碼/功能範圍）
- 使用繁體中文
- 簡短明確，避免模糊用詞（如「修復」「優化」需搭配具體對象）

## 敘述 Rules

- 使用繁體中文
- 單一變更：一句話說明，50 字內，祈使語氣
- 多項變更：body 以 `1. 2. 3.` 列點換行

## Examples

**單一變更：**
```
[認證機制] 新增用戶登入功能
```
```
[Redis] 修復連線逾時問題
```
```
[API文件] 更新接口說明
```

**多項變更：**
```
[獎品管理]
1. 新增要求兌換設定的處理函數
2. 新增要求暴雷禮包資訊的處理函數
3. 更新相關資料結構以支援新功能
```

## Workflow

1. 分析 staged changes 或 diff
2. 判斷變更數量：單一機制/元件 → 單一變更格式；跨多項 → 多項變更格式
3. 選定 Title（對應程式碼範圍，如 `[grpc]`、`[獎品管理]`、`[Redis]`）
4. 撰寫敘述，祈使語氣，50 字內
